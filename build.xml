<project name="MyProject" default="compile" basedir=".">
	<description>
		the buildfile of the complete src in the java git repos
	</description>
	<!-- set global properties for this build -->
	<property name="src" location="src"/>
	<property name="lib" location="lib"/>
	<property name="build" location="bin"/>
	<property name="doc"  location="doc"/>
	<property name="classpath"  location="lib/acm.jar:lib/junit-4.8.2.jar"/>

	<target 
		name="compile"
		description="compile the source " >
		<!-- Compile the java code from ${src} into ${build} -->
		<javac srcdir="${src}" classpath="${classpath}" destdir="${build}" debug="on"/>
		<touch file="${build}/empty"/>
	</target>

	<target name="doc"
		description="generate the javadoc" >
		<javadoc 
			sourcepath="${src}"
			destdir="${doc}"
			author="true"
			windowtitle="Java Sourcecode - Informatik Studium Tu-Darmstadt"/>
	</target>

	<target 
		name="clean"
		description="clean up" >
		<delete dir="${build}"/>
		<delete dir="${doc}"/>
		<mkdir dir="${build}"/>
		<mkdir dir="${doc}"/>
		<touch file="${build}/empty"/>
		<touch file="${doc}/empty"/>
	</target>
</project>

